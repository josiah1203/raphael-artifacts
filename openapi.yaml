openapi: 3.1.0
info:
  title: raphael-artifacts
  version: 0.1.0
  description: Artifact CRUD, metadata, lifecycle, snapshots
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
